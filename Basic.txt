/* General Styles */
body {
    font-family: 'Kavoon', cursive; /* Custom font similar to the game's title font */
    background-color: #0b0c22; /* Dark blue background color */
    color: #fff; /* White text color */
    text-align: center;
    margin: 0;
    padding: 20px;
    background-image: url('https://orteil.dashnet.org/cookieclicker/img/bgBlue.jpg'); /* Background image from the game */
    background-repeat: no-repeat;
    background-size: cover;
}

/* Title */
h1 {
    font-size: 4em;
    color: #fff; /* White for title */
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
}

/* Cookie Image */
.cookie {
    width: 256px; /* Cookie size */
    margin: 20px auto;
    border-radius: 50%;
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.5);
    cursor: pointer;
}

/* Button Styles */
.button {
    background-color: #4d2600; /* Dark brown button color */
    border: 2px solid #3b1e00; /* Slightly darker border */
    border-radius: 15px;
    padding: 15px 30px;
    font-size: 1.5em;
    cursor: pointer;
    color: #fff; /* White text on buttons */
    text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.5);
    margin: 10px;
    box-shadow: inset 0 0 5px #2d1300, 0 4px 6px rgba(0, 0, 0, 0.3);
}

.button:hover {
    background-color: #5b2f00; /* Lighter brown on hover */
}

/* Score Display */
.score {
    font-size: 2.5em;
    margin: 20px 0;
    color: #fff; /* White text for the score */
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
}

/* Upgrades Section */
.upgrade {
    background-color: #774715; /* Lighter brown for upgrade cards */
    border: 2px solid #5a3b0a; /* Darker border */
    border-radius: 10px;
    padding: 10px;
    margin: 15px;
    box-shadow: 0 2px 5px rgba(0, 0, 0, 0.3);
    color: #fff; /* White text */
    text-align: left;
    cursor: pointer;
    transition: transform 0.3s;
}

.upgrade:hover {
    transform: scale(1.05); /* Slight grow effect on hover */
}

/* Footer Styles */
footer {
    margin-top: 30px;
    font-size: 0.8em;
    color: #bbb; /* Light grey for footer text */
}

/* Animation for clicking */
@keyframes clickAnimation {
    0% { transform: scale(1); }
    50% { transform: scale(1.1); }
    100% { transform: scale(1); }
}

.clicked {
    animation: clickAnimation 0.2s ease-in-out; /* Click effect animation */
}

/* Link Styles */
a {
    color: #ffcc00; /* Golden color for links */
    text-decoration: none;
}

a:hover {
    text-decoration: underline;
}
/* General Styles */
body {
    font-family: 'Kavoon', cursive; /* Custom font similar to the game's title font */
    background-color: #0b0c22; /* Dark blue background color */
    color: #fff; /* White text color */
    text-align: center;
    margin: 0;
    padding: 20px;
    background-image: url('https://orteil.dashnet.org/cookieclicker/img/bgBlue.jpg'); /* Background image from the game */
    background-repeat: no-repeat;
    background-size: cover;
}

/* Title */
h1 {
    font-size: 4em;
    color: #fff; /* White for title */
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
}

/* Cookie Image */
.cookie {
    width: 256px; /* Cookie size */
    margin: 20px auto;
    border-radius: 50%;
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.5);
    cursor: pointer;
}

/* Button Styles */
.button {
    background-color: #4d2600; /* Dark brown button color */
    border: 2px solid #3b1e00; /* Slightly darker border */
    border-radius: 15px;
    padding: 15px 30px;
    font-size: 1.5em;
    cursor: pointer;
    color: #fff; /* White text on buttons */
    text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.5);
    margin: 10px;
    box-shadow: inset 0 0 5px #2d1300, 0 4px 6px rgba(0, 0, 0, 0.3);
}

.button:hover {
    background-color: #5b2f00; /* Lighter brown on hover */
}

/* Score Display */
.score {
    font-size: 2.5em;
    margin: 20px 0;
    color: #fff; /* White text for the score */
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
}

/* Upgrades Section */
.upgrade {
    background-color: #774715; /* Lighter brown for upgrade cards */
    border: 2px solid #5a3b0a; /* Darker border */
    border-radius: 10px;
    padding: 10px;
    margin: 15px;
    box-shadow: 0 2px 5px rgba(0, 0, 0, 0.3);
    color: #fff; /* White text */
    text-align: left;
    cursor: pointer;
    transition: transform 0.3s;
}

.upgrade:hover {
    transform: scale(1.05); /* Slight grow effect on hover */
}

/* Footer Styles */
footer {
    margin-top: 30px;
    font-size: 0.8em;
    color: #bbb; /* Light grey for footer text */
}

/* Animation for clicking */
@keyframes clickAnimation {
    0% { transform: scale(1); }
    50% { transform: scale(1.1); }
    100% { transform: scale(1); }
}

.clicked {
    animation: clickAnimation 0.2s ease-in-out; /* Click effect animation */
}

/* Link Styles */
a {
    color: #ffcc00; /* Golden color for links */
    text-decoration: none;
}

a:hover {
    text-decoration: underline;
}
